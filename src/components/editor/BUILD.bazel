package(default_visibility = ["//visibility:public"])

load("@bazel_javascript//:defs.bzl", "ts_library")

ts_library(
  name = "editor",
  srcs = [
    "Editor.tsx",
    "Editor.module.css",
  ],
  deps = [
    "//:packages",
  ],
)
